name: tests

on:
  pull_request:
    branches:
     - master
  workflow_dispatch:

jobs:
  build:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd ./auth
      - run: npm install
      - run: npm test:ci
